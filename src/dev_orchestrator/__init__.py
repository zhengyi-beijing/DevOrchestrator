"""DevOrchestrator — portable Python v1 P2-equivalent read-only compatibility layer.

The package mirrors the accepted PowerShell P2 monitor/web behavior without
starting agents, advancing phases, mutating observed repositories, or
performing hardware actions.
"""

__version__ = "0.1.0"
