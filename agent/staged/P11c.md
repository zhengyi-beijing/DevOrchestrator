# P11c Provider Context and RDC Evidence (P11-C)

Status: **PENDING DESIGN**

Owner authorization: **START P11c / 2026-09-13**

Goal: build evidence-backed diagnosis for provider/context switching, quota/failover delay, and RDC multi-project contention using the accounting substrate from P11b.

Scope:
- Record provider/account/model/session continuity from real AIBroker dispatch/result evidence; never infer unavailable attempts or first-output timestamps.
- Measure resource switches, context continuity, quota/rate-limit observations, and deterministic failover latency from correlated DevO request/result events.
- Import normalized RDC invocation evidence: command size/count, duration, first observed output when available, connection generation, cancellation/reconnect effects, and affected invocations.
- Implement deterministic classifiers for head-of-line blocking, starvation, session coupling, reconnect contamination and true deadlock using explicit thresholds and evidence fields.
- Keep project isolation: probes for one project must not cancel or mutate another project.
- Preserve unknown/unavailable values instead of synthesizing metrics.

Acceptance:
- Synthetic fixtures prove context-hit/switch and failover calculations from explicit correlation keys.
- RDC probe fixtures distinguish isolated concurrency, HOL blocking, starvation, session coupling and deadlock deterministically.
- Cross-project cancellation/reconnect tests prove one project's recovery does not corrupt another project.
- Full regression and `git diff --check` pass; commit locally only, no push.

Out of scope:
- 8770 dashboard/reporting and representative-day acceptance (P11d).
- Resuming the paused xray-hw-platform project without owner authorization.
